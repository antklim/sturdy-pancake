---
import { Astronav } from 'astro-navbar';
import sharedPropertiesLogo from '@/assets/shared-properties.svg'
import { Image } from 'astro:assets';

// const menuitems = [
//   {
//     title: 'Pricing',
//     path: '/pricing',
//   },
//   {
//     title: 'About',
//     path: '/about',
//   },
//   {
//     title: 'Contact',
//     path: '/contact',
//   },
// ];
---

<div class='mt-20'>
    <nav class='w-full top-0 left-0 z-50 fixed'>
        <header class='flex flex-col lg:flex-row justify-between items-center my-5 px-6 md:px-32'>
            <Astronav>
                <div class='flex w-full lg:w-auto items-center justify-between'>
                  <a href="/"><Image src={sharedPropertiesLogo} alt="Shared Properties" /></a>                  
                </div>
                <!-- <MenuItems class='hidden w-full lg:w-auto mt-2 lg:flex lg:mt-0'>
                  <ul class='flex flex-col lg:flex-row lg:gap-3'>
                    {menuitems.map((item) => (
                        <>                        
                            <li>
                              <a
                                href={item.path}
                                class='flex lg:px-3 py-2 items-center'>
                                <span> {item.title}</span>
                              </a>
                            </li>
                        </>
                      )
                    )}
                  </ul>
                </MenuItems> -->
            </Astronav>
        </header>
    </nav>
</div>